# AI Studio Backend

Secure proxy backend for advanced Generative AI requests (Gemini, Imagen, Veo).

- Manages server-side jobs for long-running generative tasks.
- Prevents leaking API credentials inside the Android client.
- Handles chunked upload and temporary storage of generation inputs/outputs.
